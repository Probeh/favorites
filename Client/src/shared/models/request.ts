export interface IRequest {
  // ============================= //
  email     ?: string ;
  favorite  ?: boolean;
  first_Name?: string ;
  full_Name ?: string ;
  id        ?: number ;
  last_Name ?: string ;
  page      ?: number ;
  per_Page  ?: number ;
  // ============================= //
}